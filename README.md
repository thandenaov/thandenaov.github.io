# GitHub Pages profile site

Bản cập nhật V8:
- Avatar halo mới, tích xác minh dạng rosette với vòng xoay.
- Thanh thông báo trên cùng trong suốt.
- Admin thêm/xóa thông báo.
- Preview hỗ trợ nhiều ảnh.
- 4 nút có mô tả bên dưới.
- Tải xuống / Lấy key hỗ trợ số lượng lựa chọn không giới hạn và lựa chọn con lồng nhiều cấp.
- Hướng dẫn hỗ trợ chữ, video, link hoặc cây lựa chọn lồng nhau.
- Admin lưu data.json lên GitHub bằng token.

Admin: `/admin/` — mật khẩu mặc định: `minhwuan`.
